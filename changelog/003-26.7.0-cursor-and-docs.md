---
id: 003
date: 2026-07-15
package-under-test: 26.7.0
type: change
---

# Target 26.7.0 + Cursor how-to + doc corrections

## What changed
- Bumped `<McpPackageVersion>` and every version reference to `26.7.0` across `Directory.Build.props`, `.github/workflows/integration.yml`, `README.md`, `AGENTS.md`, `examples/*`, and `how-to/*`. The suite now targets `GroupDocs.Merger.Mcp` 26.7.0 (shared `ToolError` formatter release).
- Added `how-to/07-use-with-cursor.md` + `examples/cursor-mcp.json` (Cursor uses the `mcpServers` key; documents the Windows `dnx` SSL-timeout → full `dotnet.exe` + cached-DLL workaround). Linked it from `how-to/README.md`.
- Corrected stale prose that survived from an earlier clone:
  - `docker-scripts/README.md` — replaced fabricated test names in the "Expected Output" / example sections with the real ones, fixed the test counts (19 cases total; ToolDiscovery 3), switched every `sample-docs/` reference to the actual `Files/` fixture folder, removed the bogus "licensed-only Split" skip list (Merger runs Split in evaluation mode), and de-duplicated the multi-version test loop.
  - `how-to/README.md` — the tool surface is `merge`, `split`, **and** `get_document_info` (was listed as only two); dropped the false "`split` requires a license / refuses to `Save()`" claim (Merger produces watermarked output in evaluation mode).
  - `how-to/05-use-with-vscode-copilot.md` — tool list now names all three tools.

## Why
The Tests repo carried fake test names, a two-tool listing, a `sample-docs/`-vs-`Files/` folder mismatch, and an incorrect eval-mode/licensing claim — all misleading to anyone reading the docs. The Cursor guide brings the client coverage in line with the other GroupDocs MCP Tests repos.

## Migration / impact
Docs / fixtures-wiring only — no test-logic or assertion changes. `ToolDiscoveryTests` still asserts exactly 3 tools, matching the server's `Merge` / `Split` / `GetDocumentInfo` surface.
